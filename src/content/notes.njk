---
title: Note Archive
eleventyNavigation:
  key: Note Archive
  order: 3
---

<ul role="list" class="flow">
{%- for note in collections.notes -%}
  <li>
    <h2><a href="{{ note.url }}">{{ note.title | renderContent('md') | stripOuterParagraph | safe }}</a></h2>
    <p><time datetime="{{ note.date | htmlDateString }}">{{ note.date | readableDate }}</time></p>
  {%- if note.summary -%}
    {{ note.summary | renderContent('md') | safe }}
  {%- endif -%}
  </li>
{%- endfor -%}
</ul>
